# main.sh が rsync --files-from に渡す「starter にコピーするもの」の一覧。
# ここに無いファイル/ディレクトリは day2-starter/ に入らない。
# パスは day2/ からの相対。# 始まりの行と空行は無視される。

# --- ディレクトリ（中身ごとコピー。中の新規ファイルは自動で追従する） ---
.devcontainer
.vscode
data
app

# --- これらは day2 の中身を入れず、指定したファイルだけを starter に入れる ---
pages/.gitkeep
tmp/.gitkeep

# --- ルート直下のファイル ---
.env.template
.gitignore
.python-version
CLAUDE.md
Makefile
app.py
pyproject.toml
uv.lock
