# main.sh が rsync --files-from に渡す「starter にコピーするもの」の一覧。
# ここに無いファイル/ディレクトリは day2-starter/ に入らない。
# パスは day2/ からの相対。# 始まりの行と空行は無視される。

# --- ディレクトリ（中身ごとコピー。中の新規ファイルは自動で追従する） ---
.devcontainer
.vscode
data

# --- これらは day2 の中身を入れず、指定したファイル/ディレクトリだけを starter に入れる ---
# app は random_number_mcp.py を含めず、__init__.py と advanced_rag だけ配置する
app/__init__.py
app/advanced_rag
pages/.gitkeep
tmp/.gitkeep

# --- ルート直下のファイル ---
.env.template
.gitignore
.python-version
CLAUDE.md
Makefile
app.py
pyproject.toml
uv.lock
